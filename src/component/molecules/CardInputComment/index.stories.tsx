import React from 'react';
import CommentInput from "./index"

export default {
    title: 'molecules',
    component: CommentInput,
};

export const commentInput = () => < CommentInput postComment={() => { }} />