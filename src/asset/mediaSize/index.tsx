const size = {
    tablet: '768px',
    desktop: '1024px',
    mobile:'600px',
}

export const device = {
    mobile: `(max-width: ${size.mobile})`,
    tablet: `(max-width: ${size.tablet})`,
    desktop: `(max-width: ${size.desktop})`,
};